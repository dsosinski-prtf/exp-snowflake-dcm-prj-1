manifest_version: 2
type: DCM_PROJECT

targets:
  PLATFORM:
    account_identifier: "WCCVLVP-OZC26701"
    project_name: "PLATFORM_DCM.PROJECTS.PLATFORM_PROJECT"
    project_owner: "PLATFORM_DEPLOY_ROLE"

default_target: PLATFORM

# Add feature projects here. Each project gets roles, warehouses, grants,
# and a DCM database/schema for every environment listed.
templating:
  defaults:
    projects:
      FITNESS:
        environments:
          DEV:
            warehouse_size: XSMALL
          PROD:
            warehouse_size: SMALL
      # ANALYTICS:
      #   environments:
      #     DEV:
      #       warehouse_size: XSMALL
      #     PROD:
      #       warehouse_size: MEDIUM
